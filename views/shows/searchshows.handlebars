<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Shows Found</title>
	<link rel="stylesheet" href="/public/main_style.css" />
</head>

<body>
	
	<!-- page content -->
	<h1>Shows Found</h1>
	<h2>{{showSearchTerm}}</h2>
	<main>
		
		{{#if shows.length}}
		
		<ul>
		{{#each shows}}  
			<li>
				<a href="/show/{{show.id}}">{{show.name}}</a>
			</li>
		{{/each}}
		</ul>
		
		{{else}}
			
			<p class="error" class="show-not-found">We're sorry, but no results were found for {{showSearchTerm}}.</p>
		
		{{/if}}
		
		<br><br>
		<a class="return" href="/">Make another search</a>
	</main>
	
</body>
</html>