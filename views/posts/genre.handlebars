<link rel="stylesheet" href="/public/css/site.css" type="text/css">

<head>
    <title>{{title}}</title>
</head>
<main>
    <header>
        <a href="/private">
            <h1>FILM FORAY</h1>
        </a>
    </header>
    <h2 class="genre_title">{{{givenGenre}}}</h2>

    <div class="container">

        <h4 class="h4">All Movies Based on {{{givenGenre}}}</h4>

        <ul class="hs">
            {{#each allData}}
            {{{title}}}
            {{/each}}
        </ul>
    </div>
</main>